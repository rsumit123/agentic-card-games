[alembic]
script_location = alembic
# alembic runs as a console script, which does not put the working directory on
# sys.path, so `alembic upgrade head` inside the container could not import app.
prepend_sys_path = .
sqlalchemy.url = sqlite:///./data/card_games.db
